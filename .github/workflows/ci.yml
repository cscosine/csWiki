name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [ main ]

jobs:
  checks:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.12", "3.13", "3.14"]
        deps: ["latest"]
        include:
          - os: ubuntu-latest
            python-version: "3.11"
            deps: "minimum"

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"  # ✅ built-in pip cache
          cache-dependency-path: |
            pyproject.toml
            setup.cfg
            requirements*.txt
            constraints-minimum.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ "${{ matrix.deps }}" = "minimum" ]; then
            pip install -e .[dev] -c constraints-minimum.txt
          else
            pip install -e .[dev]
          fi
        shell: bash

      - name: Run pre-commit
        run: pre-commit run --all-files

      - name: Run tests with coverage
        run: |
          pytest \
            --log-cli-level=INFO \
            --cov=toc_generator \
            --cov-branch \
            --cov-report=xml:coverage-${{ matrix.os }}-${{ matrix.python-version }}-${{ matrix.deps }}.xml \
            --cov-report=html:htmlcov-${{ matrix.os }}-${{ matrix.python-version }}-${{ matrix.deps }} \
            --cov-report=term-missing \
            | tee coverage-${{ matrix.os }}-${{ matrix.python-version }}-${{ matrix.deps }}.txt
        shell: bash

      - name: Upload coverage HTML as artifact
        uses: actions/upload-artifact@v6
        with:
          name: coverage-html-${{ matrix.os }}-py${{ matrix.python-version }}-${{ matrix.deps }}
          path: htmlcov-${{ matrix.os }}-${{ matrix.python-version }}-${{ matrix.deps }}

      - name: Add coverage to summary
        run: |
          echo "## 🧪 Coverage (${{ matrix.os }} / Python ${{ matrix.python-version }} / ${{ matrix.deps }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat coverage-${{ matrix.os }}-${{ matrix.python-version }}-${{ matrix.deps }}.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
        shell: bash
